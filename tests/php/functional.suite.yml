# Codeception Test Suite Configuration

# Suite for WordPress functional tests.
# Emulate web requests and make the WordPress application process them.


actor: FunctionalTester
modules:
    enabled:
        - WPDb
        - WordPress
        - Asserts
        - \Helper\Functional
    config:
        WPDb:
            dsn: 'mysql:host=localhost;dbname=wp_underscores_test'
            user: root
            password: ''
            dump: 'tmp/_data/dump.sql'
            populate: true
            cleanup: true
            url: '__url__'
            tablePrefix: wp_
        WordPress:
            depends: WPDb
            wpRootFolder: 'c:/wamp/www/underscores'
            adminUsername: admin
            adminPassword: 1234
            adminPath: '/wp-admin'
env:
    gitlab:
        modules:
            config:
                WPDb:
                    dsn: 'mysql:host=mysql;dbname=wordpress;'
                    password: secret
                    url: 'http://localhost'
